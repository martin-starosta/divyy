{
  "name": "@repo/cli",
  "version": "0.1.0",
  "description": "CLI that estimates dividend yield potential from free data sources",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "divvy": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "test": "echo 'No tests configured'",
    "test:watch": "echo 'No tests configured'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@repo/core": "0.1.0"
  },
  "devDependencies": {
    "@repo/typescript-config": "file:../../packages/typescript-config",
    "@repo/eslint-config": "file:../../packages/eslint-config",
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "eslint": "^9"
  }
}